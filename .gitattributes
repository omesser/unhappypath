# Normalize line endings to LF in the repo, whatever the working OS does (ADR-0012).
# This is the fix, not a check that complains after the fact.
* text=auto eol=lf

# Binaries git should not touch.
*.png binary
*.jpg binary
*.woff2 binary
